{
  "name": "pulse-path",
  "version": "1.0.0",
  "description": "Pulse-Path: Ambulance emergency response app with real-time traffic management for Bengaluru",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && node index.js",
    "frontend": "cd frontend && npm run dev",
    "start": "npm run dev",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "prepare": "cd frontend && npm install && cd ../backend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": ["ambulance", "emergency", "traffic", "bengaluru", "pulse-path"],
  "author": "shashank-905",
  "license": "MIT"
}
